---
route: "설계 완료 → 구현 시작 전 준수사항"
format: "코딩 규칙 + 호환성 체크 + TDD 프로세스"
context: "Python 3.11.7 + Git Bash 환경 일관성"
rules: "TDD 원칙 준수 (테스트 먼저, 구현 나중)"
---

# 10.6. 구현 규칙

## 환경 요구사항

**필수:**
- Python: 3.11.7 (정확히)
- Git Bash: 최신 버전
- OS: Windows / Linux / macOS

---

## 코딩 규칙

### 1️⃣ Python 3.11.7 호환성

```python
# Type hints (필수)
def process_file(filename: str) -> dict:
    pass

# f-strings (필수)
message = f"Hello, {name}"

# dataclasses (필수)
from dataclasses import dataclass

@dataclass
class Config:
    timeout: int = 30
    retry: int = 3
```

---

### 2️⃣ Git Bash 호환성

```python
# ❌ Windows 경로 직접 작성 금지
path = "C:\\Users\\user\\file.txt"

# ✅ pathlib 사용
from pathlib import Path
path = Path("C:/Users/user/file.txt")
# 또는
path = Path.home() / "file.txt"
```

---

## TDD 프로세스

**테스트 경로:** `__test__/{YYYYMMDD}_{title}/test_{module}.py`

```python
import unittest
from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).parent.parent.parent / "_scripts"))

class TestMain(unittest.TestCase):
    def test_function_returns_expected_output(self):
        result = some_function({"key": "value"})
        self.assertEqual(result["status"], "success")

if __name__ == "__main__":
    unittest.main()
```

---

## 호환성 체크리스트

- [ ] Python 3.11.7에서 실행되는가?
- [ ] Git Bash에서 실행되는가?
- [ ] 한글 입출력이 정상인가?
- [ ] 모든 테스트가 통과하는가?

---

**다음**: TDD 프로세스에 따라 구현 시작
