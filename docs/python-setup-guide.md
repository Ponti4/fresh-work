# Python 설치 및 환경 설정 가이드

> **gpters 20기 프리랜서를 위한 완전 초보자 가이드**

Week 2-3에서 Skills를 만들려면 Python이 필요합니다. 이 가이드는 **Python을 처음 접하는 분**도 따라할 수 있도록 작성되었습니다.

---

## 📋 목차

1. [Step 0: Python 설치 여부 확인](#step-0-python-설치-여부-확인) ← **여기부터 시작!**
2. [Step 1: Python 3.11 LTS 설치](#step-1-python-311-lts-설치)
3. [Step 2: 가상환경이란?](#step-2-가상환경이란)
4. [Step 3: 가상환경 생성](#step-3-가상환경-생성)
5. [Step 4: 가상환경 활성화](#step-4-가상환경-활성화)
6. [Step 5: 필수 패키지 설치](#step-5-필수-패키지-설치)
7. [Step 6: 설치 확인](#step-6-설치-확인)
8. [주의사항 & FAQ](#주의사항--faq)

**총 소요 시간: 15-20분**

---

## Step 0: Python 설치 여부 확인

**먼저 Python이 이미 설치되어 있는지 확인합니다!**

### Windows 사용자

1. **명령 프롬프트 (CMD)** 또는 **Git Bash** 열기
2. 다음 명령어 입력:

```bash
python --version
```

**또는**

```bash
python3 --version
```

### Mac 사용자

1. **터미널** 열기 (⌘ + Space → "터미널" 검색)
2. 다음 명령어 입력:

```bash
python3 --version
```

⚠️ **Mac에는 기본으로 Python 2.7이 설치되어 있을 수 있습니다. `python --version`으로 확인했을 때 2.x 버전이 나오면 무시하고 `python3 --version`을 사용하세요.**

---

### 🔍 결과 판단

#### ✅ Case 1: Python 3.11.x가 나온 경우

```
Python 3.11.7
```

→ **이미 설치되어 있습니다!** [Step 2: 가상환경이란?](#step-2-가상환경이란)으로 이동하세요.

#### ⚠️ Case 2: Python 3.10.x, 3.9.x 등 다른 버전이 나온 경우

```
Python 3.10.5
```

→ **다른 버전이 설치되어 있습니다.** 3.10 이상이면 괜찮지만, **3.11 LTS 설치를 권장합니다.** [Step 1](#step-1-python-311-lts-설치)로 이동하세요.

#### ❌ Case 3: "command not found" 또는 에러가 나온 경우

```
'python' is not recognized as an internal or external command
```

→ **Python이 설치되어 있지 않습니다.** [Step 1](#step-1-python-311-lts-설치)로 이동하세요.

---

## Step 1: Python 3.11 LTS 설치

### 📥 다운로드 링크

**권장 버전: Python 3.11.7 (2027년까지 공식 지원)**

#### Windows 사용자

**다운로드 링크:**
- **64비트 (대부분)**: [Python 3.11.7 Windows 64-bit](https://www.python.org/ftp/python/3.11.7/python-3.11.7-amd64.exe)
- 32비트: [Python 3.11.7 Windows 32-bit](https://www.python.org/ftp/python/3.11.7/python-3.11.7.exe)

**설치 방법:**

1. 다운로드한 `.exe` 파일 실행
2. ⚠️ **중요!** 첫 화면에서 **"Add Python to PATH"** 체크박스를 **반드시** 선택하세요!

   ```
   [✓] Add Python 3.11 to PATH  ← 이거 꼭 체크!
   ```

3. **"Install Now"** 클릭
4. 설치 완료 (약 2-3분 소요)

**설치 확인:**

CMD 또는 Git Bash를 **새로** 열고:

```bash
python --version
```

→ `Python 3.11.7`이 나오면 성공!

---

#### Mac 사용자

**방법 1: 공식 설치 파일 (권장)**

**다운로드 링크:**
- [Python 3.11.7 macOS 64-bit](https://www.python.org/ftp/python/3.11.7/python-3.11.7-macos11.pkg)

**설치 방법:**

1. 다운로드한 `.pkg` 파일 실행
2. 설치 마법사 따라가기 (기본값으로 진행)
3. 설치 완료 (약 2-3분 소요)

**설치 확인:**

터미널을 **새로** 열고:

```bash
python3 --version
```

→ `Python 3.11.7`이 나오면 성공!

---

**방법 2: Homebrew (이미 Homebrew를 사용하는 경우)**

```bash
# Python 3.11 설치
brew install python@3.11

# 설치 확인
python3 --version
```

---

## Step 2: 가상환경이란?

### 🤔 왜 가상환경이 필요한가요?

**비유로 설명:**

- **시스템 Python**: 집 전체에서 공유하는 공용 작업실
- **가상환경**: 프로젝트마다 독립된 개인 작업실

**가상환경이 없으면 문제:**
- ❌ 프로젝트 A에서 패키지 X 버전 1.0 설치
- ❌ 프로젝트 B에서 패키지 X 버전 2.0 설치 → **충돌!**
- ❌ 시스템 전체가 망가질 수 있음

**가상환경이 있으면:**
- ✅ 프로젝트 A: 독립된 공간에서 버전 1.0 사용
- ✅ 프로젝트 B: 독립된 공간에서 버전 2.0 사용
- ✅ 서로 충돌 없음, 안전함

### 📦 이번 스터디에서는?

`gpters-20th-templates` 프로젝트 전용 가상환경을 만들어서:
- openai-whisper (음성 전사)
- google-api-python-client (Google Calendar API)
- 기타 필요한 패키지

를 **격리된 환경**에 설치합니다.

---

## Step 3: 가상환경 생성

### 📂 프로젝트 폴더로 이동

먼저 `gpters-20th-templates` 프로젝트 폴더로 이동합니다.

```bash
cd /path/to/gpters-20th-templates
```

**예시 (Windows):**
```bash
cd C:\Users\YourName\Projects\gpters-20th-templates
```

**예시 (Mac):**
```bash
cd ~/Projects/gpters-20th-templates
```

### 🏗️ 가상환경 생성

**Windows:**
```bash
python -m venv venv
```

**Mac:**
```bash
python3 -m venv venv
```

**설명:**
- `venv` (명령어): Python 가상환경 생성 모듈
- `venv` (폴더명): 가상환경을 저장할 폴더 이름 (관례상 `venv` 사용)

**결과:**
프로젝트 폴더에 `venv/` 폴더가 생성됩니다.

```
gpters-20th-templates/
├── venv/          ← 새로 생성됨!
├── .claude/
├── docs/
└── ...
```

⏳ **소요 시간: 약 30초~1분**

---

## Step 4: 가상환경 활성화

**가상환경을 "켜는" 단계입니다.** 이 작업은 **프로젝트 작업할 때마다 해야 합니다!**

### Windows (CMD)

```bash
venv\Scripts\activate
```

### Windows (Git Bash)

```bash
source venv/Scripts/activate
```

### Mac / Linux

```bash
source venv/bin/activate
```

---

### ✅ 활성화 확인

활성화되면 명령 프롬프트 앞에 `(venv)`가 표시됩니다:

**Windows:**
```
(venv) C:\Users\YourName\Projects\gpters-20th-templates>
```

**Mac:**
```
(venv) YourName@MacBook gpters-20th-templates %
```

→ `(venv)`가 보이면 **활성화 성공!**

---

### 🔴 비활성화 방법 (작업 끝난 후)

가상환경을 "끄려면":

**모든 OS:**
```bash
deactivate
```

→ `(venv)`가 사라지면 비활성화 완료

---

## Step 5: 필수 패키지 설치

⚠️ **가상환경이 활성화된 상태**에서 진행하세요! (프롬프트에 `(venv)` 표시 확인)

### 📦 패키지 목록

Week 2-3에서 사용할 패키지들:

| 패키지 | 용도 | 사용 주차 |
|--------|------|-----------|
| `openai-whisper` | 음성 전사 자동화 | Week 2 |
| `google-api-python-client` | Google Calendar API | Week 3 |
| `google-auth-oauthlib` | Google OAuth 인증 | Week 3 |
| `python-dotenv` | .env.local 파일 읽기 | Week 3 |
| `requests` | API 호출 (범용) | Week 2-3 |

### 🚀 설치 명령어

**한 번에 모두 설치:**

```bash
pip install openai-whisper google-api-python-client google-auth-oauthlib python-dotenv requests
```

⏳ **소요 시간: 약 3-5분** (openai-whisper가 큰 패키지라 시간 소요)

---

### 📊 설치 진행 중 출력 예시

```
Collecting openai-whisper
  Downloading openai-whisper-20231117-py3-none-any.whl (801 kB)
     ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 801.2/801.2 kB 5.2 MB/s eta 0:00:00
Collecting google-api-python-client
  Downloading google_api_python_client-2.110.0-py2.py3-none-any.whl (12.0 MB)
     ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 12.0/12.0 MB 10.5 MB/s eta 0:00:00
...
Successfully installed ...
```

→ **"Successfully installed"**가 나오면 성공!

---

## Step 6: 설치 확인

### ✅ 설치된 패키지 목록 확인

```bash
pip list
```

**출력 예시:**
```
Package                    Version
-------------------------- -----------
google-api-python-client   2.110.0
google-auth                2.25.2
google-auth-oauthlib       1.2.0
openai-whisper             20231117
python-dotenv              1.0.0
requests                   2.31.0
...
```

→ 위 패키지들이 보이면 **설치 완료!**

---

### 🧪 Python 버전 확인 (가상환경 내)

```bash
python --version
```

**출력:**
```
Python 3.11.7
```

→ 가상환경 안에서도 3.11.7 사용 확인!

---

## 주의사항 & FAQ

### ⚠️ 중요한 주의사항

#### 1. **프로젝트 작업할 때마다 가상환경 활성화 필수!**

**잘못된 예:**
```bash
# 가상환경 활성화 안 하고 작업
cd gpters-20th-templates
python scripts/some_script.py  # ❌ 에러 발생!
```

**올바른 예:**
```bash
# 가상환경 활성화 후 작업
cd gpters-20th-templates
source venv/bin/activate      # Mac
# 또는 venv\Scripts\activate  # Windows
python scripts/some_script.py  # ✅ 정상 작동
```

#### 2. **gcalcli는 가상환경 밖에서 전역 설치**

gcalcli는 `pipx`로 전역 설치합니다 (Week 3에 `/setup-google-calendar` 명령어 실행 시 자동):

```bash
# gcalcli는 가상환경과 무관하게 전역으로 사용
gcalcli list  # ✅ 어디서든 사용 가능
```

---

### 💡 자주 묻는 질문 (FAQ)

**Q1. 가상환경 활성화를 깜빡했는데 어떻게 알 수 있나요?**

- 명령 프롬프트에 `(venv)`가 없으면 비활성화 상태입니다.
- `pip list`를 실행했을 때 openai-whisper 등이 안 보이면 비활성화 상태입니다.

**Q2. 가상환경을 삭제하고 싶어요.**

```bash
# 가상환경 비활성화
deactivate

# venv 폴더 삭제
rm -rf venv          # Mac/Linux
rmdir /s venv        # Windows
```

**Q3. 가상환경을 다시 만들어야 하나요?**

패키지를 잘못 설치했거나 문제가 생겼을 때:

```bash
# 1. 기존 가상환경 삭제
rm -rf venv

# 2. 새로 생성
python3 -m venv venv

# 3. 활성화
source venv/bin/activate

# 4. 패키지 재설치
pip install openai-whisper google-api-python-client google-auth-oauthlib python-dotenv requests
```

**Q4. Windows에서 "실행 정책" 에러가 나요.**

```
venv\Scripts\activate : 이 시스템에서 스크립트를 실행할 수 없으므로...
```

**해결:**

PowerShell을 **관리자 권한**으로 열고:

```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

그 후 다시 `venv\Scripts\activate` 실행

**Q5. Mac에서 "command not found: python" 에러가 나요.**

Mac에서는 `python3`을 사용하세요:

```bash
# python 대신 python3
python3 -m venv venv
python3 --version
```

**Q6. Personal Skills vs Project Skills와 가상환경의 관계는?**

- **가상환경**: Python 패키지 격리 (이 프로젝트만)
- **Project Skills**: Claude Code Skills 격리 (`.claude/skills/`)
- **Personal Skills**: 모든 프로젝트에서 사용 (`~/.claude/skills/`)

→ **다른 개념입니다!** 가상환경은 Python 패키지 관리용입니다.

**Q7. `venv/` 폴더를 Git에 커밋해야 하나요?**

**❌ 아니요!** `.gitignore`에 이미 추가되어 있습니다.

```gitignore
# .gitignore
venv/
*.pyc
__pycache__/
```

→ 가상환경은 각자 로컬에서 생성합니다.

---

## 🎯 완료 체크리스트

설정이 완료되었는지 확인하세요:

- [ ] Python 3.11.x 설치됨 (`python --version` 확인)
- [ ] 가상환경 생성됨 (`venv/` 폴더 존재)
- [ ] 가상환경 활성화 가능 (프롬프트에 `(venv)` 표시)
- [ ] 필수 패키지 설치됨 (`pip list`로 확인)
- [ ] openai-whisper, google-api-python-client 등 보임

**모두 체크되었으면 Week 2 준비 완료입니다!** 🎉

---

## 📚 다음 단계

- **Week 1**: 자동화 아이디어 발견 (`templates/automation-design.md`)
- **Week 2**: 음성 전사 Skill 실습 (`skills/voice-transcription/`)
- **Week 3**: Google Calendar API 연동 (`/setup-google-calendar`)

---

**작성일**: 2025-12-26
**업데이트**: Week 2 시작 전
